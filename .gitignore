# Build output
/build/
/out/
/cache/

# Test binaries (compiled by hand from test_*.cpp)
/test_animator
/test_weapon

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CMakeUserPresets.json

# Compile / IDE
*.o
*.obj
*.exe
*.pdb
*.ilk
.vs/
.vscode/
.idea/
.cache/
compile_commands.json

# OS
.DS_Store
Thumbs.db

# Windows release bundle: only the zip is tracked (LFS)
/dist/*
!/dist/*.zip

# Third-party asset packs and anything derived from them. Kept locally for
# reference only -- their licence restricts redistribution, so none of it is
# tracked. The tiles the game actually ships are generated from scratch by
# tools/build_levels.py and are tracked.
/archive/
